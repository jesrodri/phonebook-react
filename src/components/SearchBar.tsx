const SearchBar = () => {
  return(
    <form>
      <input
        type="text"
        className="ml-20 mt-6 p-2 rounded-md"
        placeholder="Search contacts..."
      />
    </form>
  );
};

export default SearchBar;
